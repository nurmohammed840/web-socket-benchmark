name: Rust

on:
  push:
    tags:
      - 'bench*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run benchmark
      run: cargo run -r -- -C codegen-units=1 -C opt-level=3
